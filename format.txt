  if type == 1:
        fake_email = f"{first_initial}{last}{mailFormat}" #change
        
    if type == 2:
         fake_email = f"{first}{last}{mailFormat}" #change
    
    if type == 3:
         fake_email = f"{first}.{last}{mailFormat}" #change
        
    if type == 4:
         fake_email = f"{first}{mailFormat}" #change

    if type == 5:
        fake_email = f"{first_initial}.{last}{mailFormat}" #change        
    if type == 6:
       fake_email = f"{first}_{last}{mailFormat}"     